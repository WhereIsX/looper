//state is an object
const defaultState = {

}

//Reducer should always return an object
const reducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
